# Copy this file to .env and adjust if needed.

# Where the frontend should send API requests. Must match the backend's
# PORT (see server/.env.example) — defaults line up out of the box.
VITE_API_URL=http://localhost:4000/api
